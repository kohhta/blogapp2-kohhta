.container
  = render 'tabs'
  - @articles.each do |article|
    = render 'article', article: article
    / , article: articleは_article.html.erbに引数はこれですと記述
  = link_to new_article_path do
    .float_btn
      %i.fa.fa-plus
