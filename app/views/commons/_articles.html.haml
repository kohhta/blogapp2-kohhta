-# timelines,articles_index のテンプレート
.container
  = render 'commons/tabs'
  - articles.each do |article|
    = render 'commons/article', article: article
  = link_to new_article_path do
    .float_btn
      %i.fa.fa-plus
